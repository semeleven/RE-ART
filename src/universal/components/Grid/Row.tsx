import styled from '../../lib/styled';

const StyledRow = styled.div`
    width: 100%;
    max-width: 1160;
    margin-left: auto;
    margin-right: auto;
    display: flex;
    flex-wrap: wrap;
`;

export default StyledRow;
